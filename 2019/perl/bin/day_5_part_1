#!/usr/bin/env perl

use strict;
use warnings;
use feature 'say';
use autodie;

use Advent::Intcode;
use Advent::Input;

my $input = "1\n";
open my $fh, '<', \$input;
my $s = Advent::Intcode->new( state => $Advent::Input::DAY_5, input => $fh, output => \*STDOUT );

print "$0: ";
$s->run();
