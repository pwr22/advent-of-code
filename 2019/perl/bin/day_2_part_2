#!/usr/bin/env perl

use strict;
use warnings;
use feature 'say';

use Advent::Intcode;
use Advent::Input;

my $sim = Advent::Intcode->new( state => $Advent::Input::DAY_2 );
my ($noun, $verb) = $sim->find_inputs_for(19690720);
say "$0: ", 100 * $noun + $verb;
