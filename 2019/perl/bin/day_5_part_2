#!/usr/bin/env perl

use strict;
use warnings;
use feature 'say';

use Advent::Intcode;
use Advent::Input;

my $s = Advent::Intcode->new( state => $Advent::Input::DAY_5, input => [5] );
$s->run();
say "$0: ", join ' ', $s->output->@*;
